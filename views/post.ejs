<%- include('header'); -%>
  <ul>
  <% for(var i=0; i<posts.length; i++) {%>
    <li>
      <img src="../<%= posts[i].imgPath%>">
      <%- posts[i].caption %>

      <!-- <span><i class="fa fa-heart" aria-hidden="true"></i></span>
      <span><i class="fa fa-trash" aria-hidden="true"></i></span> -->
    </li>

    <form class="" action="/comment/<%=posts[i]._id%>" method="post">
      <input type="text" name="comment" >
      <button type="submit" name="button">Submit to see</button>
    </form>

  <% } %>
  </ul>
  <div class="">
  </div>
<% for (var i=0; i<comments.length;i++){%>
   <p><span><%=comments[i].user%></span>
   <span><%=comments[i].comments%></span>

   <% if (user.local.email === posts[0].user ) { %>
     <span><i class="fa fa-trash" aria-hidden="true"></i></span>
     </p>
  <% } else { %>
  <% } %>

<%}%>
  <a href="/profile">Return to Profile</a>
  <a href="/feed">Return to Feed</a>
<%- include('footer'); -%>
